<!-- <div class="row">
<div class="col-lg-3"></div>
<div class="col-lg-6">
  {{#if success_msg}}
  <hr>
    <div class="alert alert-success">{{success_msg}}</div>
  {{/if}}

  {{#if error_msg}}
  <hr>
    <div class="alert alert-danger">{{error_msg}}</div>
  {{/if}}

  {{#if error}}
  <hr>
    <div class="alert alert-danger">{{error}}</div>
  {{/if}}
    <div class="address-box boxshadow">
        <h4>Login</h4>
      <form method="post">
          <div class="form-group">
              <input type="text" name="username" placeholder="Username" class="form-control" required/>
          </div>
          <div class="form-group">
              <input type="password" name="password" placeholder="Password" class="form-control" required/>
          </div>
          <button type="submit" class="cryptod-button-warning">Login</button><hr/>
          <div class="form-group">
              <div class="g-recaptcha" data-sitekey="6Lc-zXIUAAAAACe_rS1Q8DP7BNbly8LolGJGxcb3"></div>
          </div>

          <div style="text-align:center">Don't have an account? Register <a href="/register">here</a>.</div>
          <div style="text-align:center"> <a href="/forgotPassword">Forgot password?</a>.</div>
      </form>
    </div>

</div>
<div class="col-lg-3"></div>
</div> -->



<!-- login start here -->
<div class="login">
  <div class="container">
    <div class="col-md-12 col-sm-12 col-xs-12 box padd0">
      <div class="col-md-3 col-sm-3 col-xs-6"></div>
      <div class="col-md-6 col-sm-6 col-xs-12">
        <h5>sign UP</h5>
        <hr>
        {{#if username}}
        <div class="alert alert-danger">Username already exist consider a different one</div>
        {{/if}}
        {{#if mail}}
        <div class="alert alert-danger">Email already exist consider a different one</div>
        {{/if}}
        <!-- {{#if errors}}
              {{#each errors}}
                <div class="alert alert-danger">{{msg}}</div>
              {{/each}}
        {{/if}} -->
        <form method="post" action="/user/register">
          <div class="form-group">
            <label>Username*</label>
            <input type="text" required name="username" value="" placeholder="Example : johndoe" id="input-name"
              class="form-control" />
          </div>
          <div class="form-group">
            <label>Email*</label>
            <input type="text" required name="email" value="" placeholder="Johndoe@example.com" id="email"
              class="form-control" />
          </div>
          <div class="form-group">
            <label>password*</label>
            <input type="password" required name="password" value="" placeholder="********" id="password"
              class="form-control" />
          </div>
          <div class="form-group">
            <label>comfirm password*</label>
            <input type="password" name="confirmPassword" value="" placeholder="********" id="input-confirmpassword"
              class="form-control" />
          </div>
          <!-- <div class="links">
						<input type="checkbox"  name="credit" class="checkclass checkbox-inline"/>
						By register, I read & accept the terms.
					</div> -->
          <button type="submit" class="btn btn-primary btn-block">Register now</button>
        </form>
        <div class="donot">
          Have an account?
          <a href="/user/login">Login Now</a>
        </div>
      </div>

      <div class="col-md-3 col-sm-3 col-xs-6"></div>
    </div>
  </div>
</div>
<script>
  $(document).ready(function () {
    $("#menu").hide();
  })
</script>
<!-- login end here -->





<!--
<div class="row">
    <div class="col-lg-3"></div>
    <div class="col-lg-6">

        {{#if user}}
                <div class="alert alert-danger">Username has taken already try new</div>
        {{/if}}
        {{#if mail}}
                <div class="alert alert-danger">Email has taken already try new</div>
        {{/if}}
        {{#if errors}}
              {{#each errors}}
                <div class="alert alert-danger">{{msg}}</div>
              {{/each}}
        {{/if}}
        <div class="address-box boxshadow">
            <h4>Register</h4>
          <form action="/register" method="post">
            <div class="form-group">
                <input type="text" name="name" placeholder="Full name" class="form-control" required/>
            </div>
              <div class="form-group">
                  <input type="text" name="username" placeholder="Username" class="form-control" required/>
              </div>
              <div class="form-group">
                  <input type="email" name="email" placeholder="Email" class="form-control" required/>
              </div>
              <div class="form-group">
                  <input type="password" name="password" placeholder="Password" class="form-control" required/>
              </div>
              <div class="form-group">
                <input type="password" class="form-control" placeholder="Confirm Password" name="password2">
              </div>
              <button type="submit" class="cryptod-button-warning">Register</button><hr/>
              <div class="form-group">
                  <div class="g-recaptcha" data-sitekey="6Lc-zXIUAAAAACe_rS1Q8DP7BNbly8LolGJGxcb3"></div>
              </div>
          </form>
        </div>

    </div>
    <div class="col-lg-3"></div>
</div> -->